define(function(){
    function FacesUtil(faces, points, front) {
        this.faces = faces;
        this.points = points;
        this.front = front;
    }

    FacesUtil.prototype.visible = function() {

    }

    return FacesUtil;
})
