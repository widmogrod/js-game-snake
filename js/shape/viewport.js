define(function() {
    'use strict';

    function Viewport(x, y, width, height) {
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
    }

    return Viewport;
})
